<h1>RESTAURANTS</h1>

<ul>
  <% @restaurants.each do |resto| %>
  <div class = "items">
    <div><%= resto.name %></div>
    <div><%= resto.address %></div>
    <div><%= resto.category %></div>
    <div><%= link_to "See details", restaurant_path(resto), class: "btn btn-primary"%></li></div>
  </div>
  <% end %>
</ul>

<%= link_to "Add restaurant", new_restaurant_path, class: 'btn btn-primary' %>


